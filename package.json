{
  "name": "chronicle-proxy",
  "version": "2.0.0",
  "description": "Chronicle AI Game Master — full app + image proxy",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
